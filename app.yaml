runtime: python310
# entrypoint: gunicorn -b :$PORT main:app
# api_version: 1
# threadsafe: true

# handlers:
# - url: /static
#   static_dir: static
# - url: /.*
#   script: main.app
  
# libraries:
#   - name: ssl
#     version: latest